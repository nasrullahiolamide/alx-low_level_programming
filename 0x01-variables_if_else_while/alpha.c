#include <stdio.h>
#include <ctype.h>
int main() {
    int i;
    for (i = 0; i < 10 ; i++)
    
        printf("%d", i);
    
        
    // for (c = 'a'; c <= 'z'; ++c)
        // putchar(toupper(c));
    
    putchar('\n');
    return 0;
}
